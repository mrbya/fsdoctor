/// Filesystem entry model used by the scanner.
pub mod entry;
/// Filesystem metadata extraction and conversion.
pub mod metadata;
/// Platform-specific system helpers, because microslop...
pub mod platform;
/// Recursive filesystem scanner.
pub mod scanner;
